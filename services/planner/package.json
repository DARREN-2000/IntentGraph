{
  "name": "@intentgraph/planner-service",
  "version": "0.1.0",
  "description": "IntentGraph planner/compiler service — converts intent to WorkflowSpec",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@intentgraph/workflow-spec": "0.1.0",
    "@intentgraph/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
